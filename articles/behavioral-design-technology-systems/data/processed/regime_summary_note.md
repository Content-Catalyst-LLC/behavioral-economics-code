# Processed Data Note

The CSV outputs in this scaffold are illustrative placeholders until the Python and R workflows are run locally.

To regenerate:

```bash
python3 python/interface_regime_simulation.py
Rscript r/default_friction_retention_simulation.R
```
